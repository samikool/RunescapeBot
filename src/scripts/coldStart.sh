#!/bin/bash
#this will be a script to start after reboot. It will first need to create an initial display for the master
#it won't need to be done on a hot start since the display should remain up
#maybe it won't need to be done at all since i will kill the display on exit of the python script